# Админ панель - Управление магазином

Админ панель для управления магазином, построенная на Next.js 15 с использованием shadcn/ui и React Query.

## Возможности

- 📊 **Панель управления** - общая статистика и аналитика
- 🛍️ **Управление товарами** - CRUD операции для товаров
- 👥 **Управление пользователями** - управление аккаунтами и ролями
- 🏷️ **Управление брендами** - управление брендами товаров
- 📦 **Управление материалами** - управление материалами товаров
- 📋 **Управление заказами** - просмотр и управление заказами
- 🚚 **Доставленные заказы** - отслеживание доставки
- ⚙️ **Статусы заказов и платежей** - управление статусами
- 📈 **Аналитика** - страница `/analytics` с данными из `v_product_inventory` и `v_user_orders_summary`
- 💾 **Резервные копии БД** - страница `/database-backup` для скачивания/восстановления SQL-дампов `pg_dump` (только для роли админ)

## Технологии

- **Next.js 15** - React фреймворк
- **TypeScript** - типизация
- **Tailwind CSS** - стилизация
- **shadcn/ui** - UI компоненты
- **React Query** - управление состоянием сервера
- **React Hook Form** - управление формами
- **Zod** - валидация данных
- **Lucide React** - иконки

## Установка и запуск

1. Установите зависимости:
```bash
pnpm install
```

2. Создайте файл `.env.local` в корне проекта:
```env
NEXT_PUBLIC_API_URL=http://localhost:5000/api
```

3. Запустите сервер разработки:
```bash
pnpm dev
```

4. Откройте [http://localhost:3000](http://localhost:3000) в браузере

## Структура проекта (FSD)

```
src/
├── app/                         # Next.js маршруты, layout и провайдеры
│   ├── layout.tsx               # Корневой layout + провайдеры
│   ├── page.tsx                 # Дашборд
│   └── {entities}/page.tsx      # Остальные страницы
├── entities/                    # Слой сущностей
│   └── {entity}/
│       ├── model/hooks.ts       # React Query хуки для сущности
│       └── ui/*.tsx             # Таблицы/виджеты просмотра
├── features/                    # Слой фич
│   └── {entity}/{feature}/ui    # Формы и интерактивные элементы
├── widgets/                     # Крупные виджеты (layout, sidebar, dashboard)
│   └── {widget}/
│       ├── model/               # Бизнес-логика виджета
│       └── ui/                  # Представление
├── shared/                      # Переиспользуемые части
│   ├── api/client.ts            # API-клиент
│   ├── types/index.ts           # Общие типы
│   ├── ui/                      # Переиспользуемые компоненты shadcn/ui
│   └── lib/utils.ts             # Хелперы
└── public/
```

## API Интеграция

Админ панель интегрируется с .NET Web API через REST endpoints:

- `GET /api/Brand` - получение брендов
- `POST /api/Brand` - создание бренда
- `PUT /api/Brand/{id}` - обновление бренда
- `DELETE /api/Brand/{id}` - удаление бренда

Аналогично для всех остальных сущностей (Product, User, Order, Material, OrderStatus, PaymentStatus, DeliveredOrder).

Дополнительно:
> `GET /api/Analytics/dashboard` — агрегированные метрики (выручка, последние заказы, низкие остатки, топ клиентов) из представлений `v_product_inventory` и `v_user_orders_summary`.
>
> `GET /api/Database/backup` / `POST /api/Database/restore` — экспорт и восстановление Postgres через `pg_dump`/`psql`, которые дергает страница резервного копирования.

## Особенности

- **Адаптивный дизайн** - работает на всех устройствах
- **Темная/светлая тема** - поддержка переключения тем
- **Валидация форм** - клиентская валидация с Zod
- **Оптимистичные обновления** - мгновенный отклик UI
- **Обработка ошибок** - корректная обработка ошибок API
- **Загрузочные состояния** - индикаторы загрузки
- **Типизация** - полная типизация TypeScript

## Разработка

Для добавления новых возможностей придерживайтесь FSD:

1. Опишите типы/DTO в `src/shared/types`.
2. Добавьте методы API-клиента (если нужны новые эндпоинты) в `src/shared/api/client.ts`.
3. Создайте хуки в `src/entities/<entity>/model/` или `src/features/.../model/`.
4. Разместите UI сущностей в `src/entities/<entity>/ui/`, а пользовательские сценарии — в `src/features`.
5. Прокиньте виджеты/страницы через `src/widgets` и `src/app`.

## Лицензия

MIT
